/**
 * @fileOverview Khepri null literal.
 */
package (
    nullLiteral)
with
    import 'parse/parse' {always next Parser},
    import 'parse/text' {string}
in {

nullLiteral = (Parser, 'Null Lexer') <|
    next(
        string('null'),
        always(null));

}